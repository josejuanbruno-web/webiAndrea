# Email usado por Let's Encrypt para avisos de expiración del certificado.
CERTBOT_EMAIL=tu-email@ejemplo.com

# Solo para desarrollo local (ponerla en .env.local, NO aquí): URL del servicio
# OTP local para que el formulario no apunte a producción.
# VITE_OTP_API_URL=http://localhost:18080
